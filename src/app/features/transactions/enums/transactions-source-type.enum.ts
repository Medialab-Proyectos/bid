export enum TransactionsSourceType {
  Idb = 0,
  LocalCounterPart,
  CoFinancing,
}

export enum TransactionsSource {
  Idb = 'BID',
  LocalCounterPart = 'LOCAL_COUNTERPART',
  CoFinancing = 'CO_FINANCING',
}
